const mongoose = require('mongoose');

const Driver_routes_Schema = new mongoose.Schema({
        _id : String,
        existAuto: Boolean,
        route: Array,
    }
);

module.exports = mongoose.model('driver_routes', Driver_routes_Schema);
